dist: trusty
sudo: false
language: node_js
node_js:
  - 8
cache:
  directories:
    - node_modules
branches:
  only:
    - master
    - /^greenkeeper/.*$/
notifications:
  email: false
install:
  - npm install
script:
  - npm run cover
  - npm run build
  - npm run semantic-release